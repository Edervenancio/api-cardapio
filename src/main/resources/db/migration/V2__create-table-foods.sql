CREATE TABLE foods (
                       id INT AUTO_INCREMENT PRIMARY KEY,
                       title VARCHAR(255),
                       image VARCHAR(255),
                       price INT
);